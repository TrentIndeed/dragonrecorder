# Shell scripts must stay LF — bash cannot parse CRLF (\r: command not found)
*.sh text eol=lf
# PowerShell is happy with CRLF
*.ps1 text eol=crlf
*.bat text eol=crlf

# RNNoise weights: never touch the line endings
*.rnnn -text
